#include <stdio.h>

int main() {
    printf("this is main.\n")

    // the first change in master branch
    int num = 0;

    // the first change in dev branch
    int data = 1;

    //
    int age = 230;

    return 0;
}